# Stage 2/2 of the SafeRLHF compliance eval: OpenAI-API grading.
# Needs network + OPENAI_API_KEY -> run on a login node (NOT a compute node), e.g.:
#   module load python-waterboa/2025.06 && source .venv/bin/activate
#   OPENAI_API_KEY=... PYTHONPATH=. python -c "from src import run; run('configs/llava-saferlhf-grade.yaml')"
# No VLM/GPU is loaded; it only reads the generations from stage 1.


- output_dir: trials/llava-saferlhf-grade-image01-llava
  task_type: VLM-SafeRLHF-grade
  generations: trials/llava-saferlhf-gen-image01-llava/generations.json
  judge:
    name: gpt-4o-mini
    workers: 8


- output_dir: trials/llava-saferlhf-grade-image01-visualrwkv
  task_type: VLM-SafeRLHF-grade
  generations: trials/llava-saferlhf-gen-image01-visualrwkv/generations.json
  judge:
    name: gpt-4o-mini
    workers: 8