# Adv-VLM — Python dependencies
# Python 3.13 (MPCDF Raven: `module load python-waterboa/2025.06`).
# Create the environment with:
#   module load python-waterboa/2025.06
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt

torch>=2.5.0               # first release with Python 3.13 wheels
torchvision>=0.20.0        # fast (tensor-based) CLIP image processor backend
transformers>=5.0.0,<5.18   # v5 API for AutoModelForMultimodalLM; capped because
                           # llava.py calls cache.crop() with a positive value,
                           # which 5.18 removes (deprecation warning as of 5.16)
accelerate>=0.33.0         # device_map / efficient model loading
datasets>=2.20.0           # benchmark loading (e.g. ImageNet-1k val)

pillow>=10.0.0             # image inputs
sentencepiece>=0.2.0       # LLaMA/LLaVA tokenizer
protobuf>=4.25.0           # tokenizer/model serialization

numpy>=1.26.0
pyyaml>=6.0
tqdm>=4.66.0
python-dotenv>=1.0.0

ipykernel>=6.29.0

ninja>=1.11.0
huggingface_hub>=0.24.0
tensorboard>=2.16.0
openai>=1.40.0
